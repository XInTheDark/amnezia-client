# AGENTS.md

## Project Scope

- The user is developing on macOS.
- This development branch is only for the macOS client and the backend/service code that the macOS client uses.
- Keep reviews, fixes, and testing focused on macOS behavior unless the user explicitly asks to inspect another platform.

